# 收尾：lite 研究範圍關閉與 v0.3.0 release（2026-09-15）

狀態：五策略第一輪比較（v0.2-lite、v0.2.1、v0.3）已完成並在 `main`。本文記錄三件收尾事：README 的「怎麼讀」段落、長版說明影片的存放方式、GitHub 頁面描述的更新。其中 push、建立 release、改描述三個公開動作由 owner 執行，本文只列命令。

## 1. 關閉的範圍

- 已完成並核對：entropy／margin（兩種訓練長度規則各 3/3）、hybrid（3/3）、core-set（2/3，不一致）、兩種規則各三個全標籤參考基線、同機重播離散度尺度（audit 一對 0.0079；v0.3 十二對最大 0.0075）。每輪的結果報告、跨 seed summary 與磁碟核對腳本都在 `docs/results/` 與 `docs/verification/`。
- 未排程（各報告「下一步」列過）：類別層級的重播離散度、core-set 不一致的追查、40% 預算、第二國別、英文 README、長版影片的 v0.3 章。這些都是「可以更好」，不是「不做就站不住」；每項要 2 到 6 小時 GPU 加一份新協定。
- 再開一輪的條件：有具體外部理由（投稿、面談被問到某一點），且先寫新的預先登記協定；沒有理由就不開。

## 2. 長版說明影片

| 項目 | 值 |
|---|---|
| 檔案 | `<evidence-root>\media\val-loop-long-20260911T1331Z.mp4` |
| 大小 | 7,718,356 bytes |
| SHA-256 | `a6cd495c75556d5e62419db4f58e869438554613cd53065e4e39b97cc72a36cb` |
| 格式 | h264，1920×1080，60 fps，109.7 秒 |
| 內容 | 短版四段接三章（訓練長度規則、全標籤參考基線、最低類別 recall 的負面結果）；分鏡在 [長版規格](../media/manim/design-long.md) |

存放決定：掛在 GitHub Release `v0.3.0` 的附件，不進 git。理由：repo 目前 private，release 與 repo 同可見度，日後改公開時附件一起公開；`<workspace>` 沒有可掛影片的公開作品集頁面；7.7 MB 遠低於附件上限。README 的動畫段落已連到該 release。

## 3. GitHub 頁面描述

目前：`Private backup: vision active-learning loop, wave-0 model contract (design retained; branch archived, not a public release)`。

改為：`Active-learning strategy comparison for RT-DETR on RDD2022 Czech: random / entropy / margin / core-set / hybrid, 3 seeds each, pre-registered decision rules, per-fit receipts, disk verification. Lite research complete (v0.3, 2026-09-12); wave-0 branch archived.`

## 4. owner 要執行的命令（依序，任一 PowerShell）

先推 `main`（含本文與 README 更新）：

```powershell
git -C "<repo>" push origin main
```

建立 release `v0.3.0`（tag 打在推上去的 `main`，影片當附件）：

```powershell
gh release create v0.3.0 --repo kuotunyu/vision-active-learning-loop --target main --title "v0.3.0: five-strategy first-round comparison complete" --notes "Lite active-learning research closed: entropy/margin 3/3 under both training-length rules, hybrid 3/3, core-set 2/3 (inconsistent), full-label references, same-machine replay scale. Results in docs/results/, verification in docs/status/. Asset: 110 s Manim explainer (1080p60), sha256 a6cd495c75556d5e62419db4f58e869438554613cd53065e4e39b97cc72a36cb." "<evidence-root>\media\val-loop-long-20260911T1331Z.mp4#val-loop-long-20260911T1331Z.mp4 (110 s, 1080p60)"
```

改頁面描述：

```powershell
gh repo edit kuotunyu/vision-active-learning-loop --description "Active-learning strategy comparison for RT-DETR on RDD2022 Czech: random / entropy / margin / core-set / hybrid, 3 seeds each, pre-registered decision rules, per-fit receipts, disk verification. Lite research complete (v0.3, 2026-09-12); wave-0 branch archived."
```

## 5. 本次沒有做的事

- 沒有動 `src/`、`tests/`、`scripts/`，沒有重跑任何 GPU 工作，沒有修改任何協定的 §1–§3。
- 沒有在 git 裡放影片。

## 6. 公開前檢查（2026-09-15，repo 仍為 private）

用 `git log -p --all` 掃整個歷史（207 個 commit；遠端只有 `main`）：

- token 與私鑰樣式（GitHub、Hugging Face、OpenAI、AWS、Slack、PEM）：0 筆。
- 電子郵件：只有 GitHub noreply 位址與測試用的 `fixture@example.invalid`。
- 追蹤中的檔案沒有 `.env`、金鑰、憑證。
- 家目錄路徑 `C:\Users\<user>\Desktop\...` 出現在 5 份 2026-08 的 Wave 0 計畫文件裡；它們是歷史紀錄，也已在 commit 歷史中，公開會露出 Windows 使用者名稱。改寫歷史不值得，owner 知悉即可。
- 資料授權：data card 以 CC BY-SA 4.0 對待 RDD2022，影像與標註不在 repo；模型授權由 `assets verify` 的登記檔記錄。
- **缺 LICENSE 檔**：公開前要由 owner 選程式碼授權（建議 Apache-2.0，與 RT-DETR、DINOv2 相同）。
- README「分支與位置」原寫 Wave 0 分支「已推到私人 GitHub」；實際遠端只剩 `main`，已改正。

公開與否、選哪個授權，都是 owner 的決定，本文不做。

## 7. 證據備份（2026-09-15）

`<evidence-root>`（lite 27 GB、wave0 12 GB、media 14 MB）在 D:（SATA 硬碟）只有一份。本日以 robocopy 複製到另一顆實體磁碟 C:（NVMe）`<backup-root>\vision-active-learning-loop-artifacts`，之後逐檔 SHA-256 比對；結果補在下方。這只防單顆磁碟故障，不防整台機器遺失；離機備份仍是 owner 的事。

結果（artifacts）：robocopy 720 個目錄、1,871 個檔案、37.585 GB，0 個失敗，4 分 51 秒；之後用 [2026-09-15-backup-verification.py](../verification/2026-09-15-backup-verification.py) 逐檔比對名稱、大小與 SHA-256，1,871 個檔案、40,357,195,913 bytes 全部一致（395 秒），輸出在 [2026-09-15-backup-verification-artifacts.json](../verification/2026-09-15-backup-verification-artifacts.json)。

結果（data）：`<data-root>`（RDD2022 Czech 影像與標註，可重新下載，但 manifest 的雜湊綁定這份）也複製到 `<backup-root>\vision-active-learning-loop-data`：5,662 個檔案、12.784 GB，0 個失敗，2 分 22 秒；逐檔比對 5,662 個檔案、13,727,135,519 bytes 全部一致（234 秒），輸出在 [2026-09-15-backup-verification-data.json](../verification/2026-09-15-backup-verification-data.json)。

## 8. 測試套件整理（2026-09-15）

- 全套 `pytest` 從 2026-09-09 的「773 passed、569 failed」變成「997 passed、17 skipped、0 failed」（153 秒）。
- 569 個失敗全來自兩個 Wave 0 啟動器測試檔找不到 `pwsh`（PowerShell 7）；現在這兩檔在 PATH 沒有 `pwsh` 時用 `pytest.skip(allow_module_level=True)` 整檔 skip，理由可見。
- 另外發現一個 v0.2.1 之後才出現的失敗：`tests/probes` 的 TF32 測試刪掉 `CUBLAS_WORKSPACE_CONFIG` 後呼叫 `configure_determinism`，假設 CUDA 尚未初始化；但 `tests/lite/test_baseline.py::test_baseline_main_records_a_fit_failure_on_disk` 在它之前先初始化了 CUDA（用一個記錄第一次初始化的 pytest plugin 找到的）。改法：CUDA 已初始化時改為設定工作區再呼叫，只驗證不設定；`configure_determinism` 本身沒有改。
- 一個雙程序競賽測試（`tests/artifacts/test_receipts.py`）在機器同時跑 robocopy 與雜湊比對時超過 20 秒 join 而失敗，單獨重跑與空閒時全套都通過；沒有改它。

## 9. 公開與授權的決定（2026-09-15，owner 委託判斷）

- 判斷：公開。作品集 repo 不公開就沒有讀者；§6 的掃描沒有需要先處理的東西；影像、標註、權重、checkpoint 都不在 repo；家目錄路徑只露出 Windows 使用者名稱，且已在歷史中。
- 授權：Apache-2.0。與 RT-DETR、DINOv2 的授權相同，含專利授權條款；owner 其他 lab 型公開 repo（vllm-single-gpu-slo-lab、grpo-rlvr-reasoning、CareRisk-48H）也用它。`LICENSE` 是 GitHub 提供的標準全文，附錄的版權行填 `2026 kuotunyu`。
- 公開的動作由 owner 執行，因為公開後已被看過或快取的內容收不回來：

```powershell
gh repo edit kuotunyu/vision-active-learning-loop --visibility public --accept-visibility-change-consequences
```

- 公開後：portfolio 工作區的 `PROJECTS.json` 與 `README.md` 把本專案的 visibility 從 private 改為 public。

## 10. 本機路徑外洩的修補（2026-09-15，公開後數小時內）

owner 在公開的 README 看到 `D:\...` 絕對路徑後指出問題。§6 的掃描只標了使用者名稱，把磁碟路徑判為無害，這個判斷錯了：公開 repo 不該出現任何本機路徑。修補內容：

- `docs/` 下 68 個檔案（結果報告、狀態文件、協定、2026-08 的 Wave 0 計畫與規格、核對輸出 JSON、發布的 experiment receipt）以固定對照表把路徑前綴換成占位符：repo 目錄 → `<repo>`、工作區 → `<workspace>`、私有證據目錄 → `<evidence-root>`、資料目錄 → `<data-root>`、備份目錄 → `<backup-root>`、使用者名稱 → `<user>`。只換前綴，其餘內容不動；歷史文件的敘述不變。
- `scripts/run_lite_seed17.ps1` 不再內建路徑：五個輸入參數預設為空，從 git 忽略的 `scripts/local-paths.ps1` 取得（範本 `scripts/local-paths.example.ps1`）；明確參數優先；缺路徑時 exit 3 並列出缺哪幾個。測試改為與本機設定檔隔離，另加兩個測試（無設定檔 → exit 3；設定檔補齊 → dry-run 通過）。
- `docs/status/` 的四支核對腳本改讀 `VAL_EVIDENCE_ROOT`、`VAL_DATA_ROOT`（由 local-paths 設定），輸出的 JSON 只寫占位符；v0.3 核對重跑三個 seed 仍 PASS。
- `docs/media/manim/render.ps1` 的 mp4 外部複製改為選用參數 `-EvidenceMediaDir`。
- README 的命令改為相對於 repo 根目錄，證據路徑用 `<evidence-root>`。
- 沒有動 `src/`、`tests/gates/`、Wave 0 的三支啟動器（它們只含 `D:\vision-active-learning-loop-artifacts\wave0` 這類以專案命名的預設值，不含使用者名稱或工作區結構；其測試沒有 `pwsh` 跑不了，不改）。

沒有修補的部分：git 歷史裡仍有原始路徑（含 5 份計畫文件裡的使用者名稱）。要清掉只能改寫歷史並強制推送，且 release `v0.3.0` 的 tag 要重打；這是 owner 的決定，本文不做。

## 11. 歷史改寫（2026-09-15）

owner 決定把 git 歷史裡的本機路徑也清掉。做法：在乾淨的 clone 上以 `git filter-repo --replace-text` 改寫全部 commit，對照表與 §10 相同（每個前綴各有反斜線、跳脫反斜線、正斜線三種形式，共 25 條，另加 2 條處理 2026-09-15 反斜線被吃掉後殘留控制字元的變體）；作者、日期、commit 訊息不變，只有檔案內容變。

- 結果：212 個 commit（原 213；「repair two path separators」那個 commit 改寫後內容為空，被移除）。HEAD 的 tree 與改寫前相同（`a44d7667…`），全歷史 `git log -p --all` 找不到任何對照表前綴、使用者名稱或控制字元。
- 所有 commit id 都變了；本文與其他文件裡引用的舊 id（`c834fb2`、`7cc404c` 等）已不存在，只當歷史敘述讀。tag `v0.3.0` 改指向改寫後的對應 commit；GitHub 上的 release 刪除重建，影片重新上傳。
- 改寫前的完整備份（git bundle）留在 owner 本機的暫存目錄，不進 repo。
- 強制推送後，舊 commit 在 GitHub 上不再可達，但可能仍可用 SHA 直接存取一段時間；公開期間若有人 clone，其本機仍有舊歷史。要徹底清除，向 GitHub Support 申請清理。

## 12. 文件整理（2026-09-15）

- 目錄：三份預先登記協定移到 `docs/protocols/`；`docs/status/` 拆成 `docs/verification/`（核對腳本與輸出）與 `docs/decisions/`（本文與復活評估）；原 8-wave 設計、計畫索引與八份 wave 計畫、Wave 0 runbook、環境邊界、A11 離線診斷移到 `docs/archive/wave0/`，並加一份索引；動畫的兩份分鏡規格移到 `docs/media/manim/`。
- 移除 71 份：A2–A11 的 39 份計畫與 28 份復原設計、lite 的 3 份已完成實作清單、v0.2.1 交接文件。整理前的狀態保留在 tag `pre-docs-reorg-2026-09-15`。
- 搬移檔的相對連結依新位置改寫；結果報告只改連結路徑，內容不動。
- 沒動 `src/`、`tests/` 與 Wave 0 啟動器：lite 依賴 `artifacts`、`models`、`probes`、`training`，`probes` 再依賴 `gates` 與 `environment`；Wave 0 啟動器與其測試保留原始路徑字串。
- 套件版本 0.1.0 改為 0.3.0，與 release 一致。
- README 的文件索引改為依目錄列出，從 16 列減為 12 列。
- 本文 §4、§10、§11 裡的舊路徑與舊 commit id 是當時的紀錄，不改。
